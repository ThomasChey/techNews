<!--menu mobile-->
<nav class="menu-res hidden-lg hidden-md ">
    <div class="menu-res-inner">
        <ul>
            <li><a href="{{ path('acccueil') }}">Accueil</a></li>
            {% for categorie in categories %}

                <li>
                    <a href="{{ path('default_categorie',{'slug' : categorie.slug}) }}">{{ categorie.nom }}</a>
                </li>

            {% endfor %}

        </ul>
    </div>
</nav>
<div class="page">
    <div class="container">
        <!--header-->
        <header class="header">
            <div class="row">
                <div class="col-md-3 col-sm-4 col-xs-12">
                    <a href="{{ path('acccueil') }}" class="logo">
                        <img alt="Logo" src="{{ asset('images/logo.png') }}"/>
                    </a>
                </div>

                <div class="col-md-3 col-md-offset-6 text-center" style="font-size: 1.5em; margin-top: 1.5em;">
                {% if  is_granted('IS_AUTHENTICATED_FULLY') %}

                    <a class="btn btn-default aller deconnexion" href="{{ path('membre_deconnexion') }}" style="">Déconnexion</a>

                {% else %}

                    <a class="btn btn-default aller connexion" href="{{ path('membre_connexion') }}" style="">Connexion</a>

                    <a class="btn btn-default aller inscription" href="{{ path('membre_inscription') }}" style="margin-left: 5%;">Inscription</a>

                {% endif %}
                </div>

            </div>
        </header>
        <!--menu-->
        <nav class="menu font-heading">
            <div class="menu-icon hidden-lg hidden-md">
                <i class="fa fa-navicon"></i>
                <span>MENU</span>
            </div>
            <ul class="hidden-sm hidden-xs">
                <li>
                    <a href="{{ path('acccueil') }}">Accueil </a>
                </li>

                {% for categorie in categories %}

                    <li>
                        <a href="{{ path('default_categorie',{'slug' : categorie.slug}) }}">{{ categorie.nom }}</a>
                    </li>

                {% endfor %}

            </ul>
            <div class="search-icon">
                <div class="search-icon-inner">
                    <i class="fa fa-search"></i>
                </div>
                <div class="search-box">
                    <input type="text" placeholder="Rechercher..."/>
                    <button>Lancer</button>
                </div>
            </div>
        </nav>